{
  "scripts": {
    "dev": "wrangler dev src/index.ts --port 3001",
    "deploy": "wrangler deploy --minify src/index.ts"
  },
  "dependencies": {
    "hono": "^4.0.8",
    "hono-mongodb-atlas": "^0.0.5",
    "mongo-http": "^0.0.20"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240208.0",
    "wrangler": "^3.25.0"
  }
}
